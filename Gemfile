# frozen_string_literal: true

source 'https://rubygems.org'

gem 'github-pages', group: :jekyll_plugins
gem 'rubocop',      group: :development

# This is needed to run a server locally
gem 'webrick'
